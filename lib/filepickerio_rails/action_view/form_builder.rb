module FilepickerioRails
  class ::ActionView::Helpers::FormBuilder
    
    # Returns a input tag tailored for a Filepicker.io upload widget to be attached for the form object
    def filepickerio_upload(method, text=nil, options={})
      # @template.filepickerio_upload_tag(object_name, text, value, objectify_options(options))
    end

  end
end